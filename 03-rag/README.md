# 03. RAG · Vector DB

CXP Agent 의 본질은 RAG 다. 화면만 만들더라도 **"왜 이 답이 나왔는지"** 를 팀과 대화할 수 있어야 한다.

| 문서 | 한 줄 |
| --- | --- |
| [01. 파이프라인](01-pipeline.md) | 인덱싱 · 검색 · 생성 전체 흐름과 각 단계의 실패 |
| [02. 청킹과 임베딩](02-chunking-embedding.md) | 품질의 8할이 여기서 결정된다 |
| [03. 검색 품질](03-retrieval-quality.md) | 하이브리드 · 리랭킹 · 쿼리 재작성 · 평가 |
| [04. 엔터프라이즈 RAG](04-enterprise-rag.md) | 권한 · 멀티테넌시 · 프롬프트 인젝션 · 운영 |

이 챕터의 **02번 3-2절·5-2절**과 **03번 4절**은 추측이 아니라
[`labs/lab2-mini-rag/`](../labs/lab2-mini-rag/) 를 실제로 돌려서 나온 **측정 결과**다.
청크 크기를 바꿨을 때 숫자가 어떻게 움직이는지, 의미 검색이 실패할 때 무엇이 검색되는지가
점수까지 그대로 들어 있다. **실습을 돌리지 않아도 그 세 절만 읽으면 결론은 같다.**

---

## 한 문장 정의

> 모델을 다시 학습시키는 대신, **질문이 들어올 때마다 관련 문서를 찾아 프롬프트에 근거로 끼워 넣어** 답하게 하는 방식.

## 왜 파인튜닝이 아닌가

| | 파인튜닝 | RAG |
| --- | --- | --- |
| 문서가 바뀌면 | 재학습 | 인덱스만 갱신 |
| 출처 표시 | 불가능 | 가능 ← **엔터프라이즈에서 이게 결정적** |
| 사용자별 권한 분리 | 사실상 불가 | 검색 필터로 가능 |
| 비용 | 크다 | 질의당 검색 + 토큰 |
| 잘하는 것 | 말투·형식·도메인 어휘 학습 | **사실(fact) 주입** |

**둘은 경쟁 관계가 아니다.** 파인튜닝은 *어떻게 말할지*, RAG 는 *무엇을 말할지*를 다룬다.
"우리 회사 휴가 규정"은 영원히 RAG 의 영역이다.

## 반드시 갖고 갈 감각 세 가지

1. **RAG 실패의 대부분은 LLM 이 아니라 검색(retriever)의 실패다.**
   답이 이상하면 모델 탓하기 전에 *어떤 청크가 들어갔는지* 부터 본다.

2. **품질을 가르는 건 모델이 아니라 청킹 전략과 검색 품질이다.**
   GPT-4o 를 Claude 로 바꿔서 해결되는 문제는 거의 없다.

3. **측정하지 않으면 조용히 나빠진다.**
   "잘 되는 것 같다"로 운영하면, 문서가 늘면서 검색 품질이 서서히 무너지는 걸 아무도 모른다.
