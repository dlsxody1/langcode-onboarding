# 01. 백엔드 근본 지식

프레임워크 이전의 것들. **ASP.NET Core 든 Spring 이든 FastAPI 든 똑같이 적용되는 층위**만 모았다.
여기를 건너뛰고 C# 문법부터 보면, 코드는 쓸 수 있는데 왜 그렇게 쓰는지를 모르는 상태가 된다.

| 문서 | 한 줄 | 왜 랭코드에서 필요한가 |
| --- | --- | --- |
| [01. 요청 하나의 수명주기](01-request-lifecycle.md) | HTTP 요청이 서버에서 겪는 일 전부 | 미들웨어 파이프라인은 ASP.NET Core 의 심장이다 |
| [02. 계층 구조와 DI](02-layers-and-di.md) | Controller-Service-Repository 는 왜 나뉘어 있나 | 공고에 "서비스(DI) 로직"이 그대로 적혀 있다 |
| [03. 데이터베이스](03-database.md) | 인덱스·트랜잭션·N+1·커넥션풀 | EF Core 가 만들어내는 문제는 전부 이 층에서 생긴다 |
| [04. 인증과 인가](04-auth.md) | 누구인가(authn) vs 뭘 할 수 있나(authz) | 엔터프라이즈 = 권한이 전부. RAG 권한 필터링의 전제 |
| [05. 오래 걸리는 작업](05-long-running-jobs.md) | 에이전트 실행처럼 30초 넘는 일을 API 로 어떻게 | AI 제품 백엔드 설계의 기본기 |

## 먼저 잡고 갈 관점

프론트엔드에서는 **한 사용자의 한 화면**이 세계의 전부다. 상태는 브라우저 탭 하나에 갇혀 있고,
내가 망쳐도 새로고침하면 사라진다.

백엔드는 정반대다.

1. **동시에 수천 개가 돈다.** 내가 짠 함수는 지금 이 순간 500번 병렬로 실행 중이다. 전역 변수에 값을 담는 순간 버그다.
2. **상태가 영구히 남는다.** 잘못 쓴 데이터는 새로고침으로 안 사라진다. 그래서 트랜잭션과 검증이 있다.
3. **신뢰 경계가 여기다.** 프론트에서 한 모든 검증은 UX 일 뿐이다. 진짜 차단은 서버에서만 일어난다.
4. **호출자를 모른다.** 내 API 를 부르는 게 우리 프론트인지, 고객사 배치 스크립트인지, 재시도 중인 큐인지 모른다.

이 네 가지가 백엔드 코드가 프론트 코드보다 방어적이고 계층이 많은 이유의 전부다.
앞으로 나올 모든 장치(DI, 트랜잭션, 멱등성, 미들웨어)는 이 넷 중 하나에 대한 대응이다.
